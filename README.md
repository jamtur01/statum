# Statum

**Statum** is a [sinatra](http://www.sinatrarb.com) app/middleware that can be run standalone app or required as a gem.

## Running standalone
Simply run the ``bundle`` and then ``rackup`` commands.

## Bundling as a gem
    gem build statum.gemspec
    sudo gem install statum-0.0.1.gem